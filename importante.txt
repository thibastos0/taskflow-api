1. Criar o ambiente virtual
python3 -m venv .venv
2. Ativar
source .venv/bin/activate
3. Criar arquivo requirements
requirements.txt
4. Instalar dependências
pip install -r requirements.txt

Obs.: deactivate para para o ambiente
